// Layout sin decoración para la página de login del superadmin
export const dynamic = 'force-dynamic'

export default function SuperadminLoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
